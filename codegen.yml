schema: https://rewarder-api-gateway-prod.cycler.cc/graphql
generates:
  ./src/generated/apollo.ts:
    documents: './src/**/*.graphql'
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    config:
      withHooks: true
      withRefetchFn: true
      scalars:
        Date: string
